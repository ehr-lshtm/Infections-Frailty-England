hes_diagnosis_hosp <- open_dataset(sources = "Z:/GPRD_GOLD/Kwabena/24_004305_type_2_HES/hes_diagnosis_hosp_24_004305_DM.txt",
format = "tsv") |>
collect() |>
arrange(patid,spno,admidate) |> to_duckdb()
library(tidyverse);library(janitor);library(magrittr);
library(purrr);library(pryr);library(glue);library(rio);
library(arrow);library(duckdb);library(collapse)
# diagnosis by hospitalization
# thus, all diagnoses made in one hospital admission
hes_diagnosis_hosp <- open_dataset(sources = "Z:/GPRD_GOLD/Kwabena/24_004305_type_2_HES/hes_diagnosis_hosp_24_004305_DM.txt",
format = "tsv") |>
collect() |>
arrange(patid,spno,admidate) |> to_duckdb()
# diagnosis by hospitalization
# thus, all diagnoses made in one hospital admission
hes_diagnosis_hosp <- open_dataset(sources = "Z:/GPRD_GOLD/Kwabena/24_004305_type_2_HES/hes_diagnosis_hosp_24_004305_DM.txt",
format = "tsv") |>
collect() |>
arrange(patid,spno,admidate) |> to_duckdb()
hes_diagnosis_hosp
library(tidyverse);library(janitor);library(magrittr);
library(purrr);library(pryr);library(glue);library(rio);
library(arrow);library(duckdb);library(collapse);library(gtsummary)
#'load libraries
library(tidyverse);library(janitor);library(magrittr);
library(purrr);library(pryr);library(glue);library(rio);
library(arrow);library(duckdb);library(collapse);library(gtsummary)
# Load datasets
source("J:/EHR-Working/Kwabena/Infections-Frailty-CPRD/paths/path_create_tidy_data.R")
collect(observations[[1]]) |>
fselect(patid,obsdate,medcodeid)
# names of deficits
deficits_names <- map_chr(eFI_deficits_codelists_file_paths, ~str_extract(.x,pattern = "(.*/)(.*)( .* codelist.csv$)",group = 2))
# named list of codelits for 35 deficits
deficits_code_list <- setNames(map(eFI_deficits_codelists_file_paths, ~unique(pull(collect(open_csv_dataset(.x)),medcodeid))),
deficits_names)
# all codelits combined
deficits_code_df <- map(deficits_code_list,~as.data.frame(.x)) |> reduce(rowbind) |>
rename_with(~"medcodeid")
eFI_deficits_codelists_file_paths
eFI_deficits_codelists_file_paths <- list.files("./codelists/2. OUTCOME - eFrailty Index - AURUM",pattern = ".csv",full.names = T)
eFI_deficits_codelists_file_paths
ethnicity_codes
